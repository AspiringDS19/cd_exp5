FIRST and FOLLOW Set Generator

📌 Description
This project automates the calculation of FIRST and FOLLOW sets for a given Context-Free Grammar (CFG). These sets are the mathematical foundation for constructing predictive parsers like LL(1).

📝 Key Definitions
FIRST(X): The set of all terminal symbols that can appear as the first symbol in any string derived from X.
FOLLOW(A): The set of terminals that can immediately follow the non-terminal A in any derivation sequence. This includes the $(EOF) symbol.

🚀 How to Run
Open script.py.
Input your grammar using the dictionary format provided in the code.

Run the script:
python script.py
